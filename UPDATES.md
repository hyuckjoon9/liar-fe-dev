# Updates - 최근 변경 사항 기록

## [2026-06-16] VOTE_RESULT 복원 및 다음 라운드 진입 기능 구현

### 1. 목적
- 투표 결과 화면(`VOTE_RESULT` 단계)에서 사용자가 새로고침을 하더라도 투표 결과(최다 득표자, 탈락 여부 등)가 그대로 복원되도록 처리합니다.
- 투표 결과 확인 후 로컬 상태에서 턴을 넘기던 기존 방식 대신, 백엔드 서버에 명확히 `next-round` 명령을 송신하여 서버의 정식 스케줄에 맞춰 다음 라운드에 진입하도록 연동합니다.

### 2. 변경 이유
- 새로고침 시 이전에 진행 중이던 투표 결과 정보가 유실되어 화면이 게임 진행 중 혹은 발언 대기 상태로 깨지는 현상이 발생했습니다.
- 클라이언트가 임의로 턴을 변경하는 것은 서버 상태와의 정합성 불일치를 초래하므로, 서버의 `/pub/rooms/{roomCode}/next-round` 엔드포인트 발행을 통한 라운드 제어 단일화가 필요했습니다.

### 3. 영향 범위
- **`App.jsx`**:
  - `loadRoom()` REST 호출 결과에 `nextRoom.game.phase === 'VOTE_RESULT'` 조건을 매핑하여 `voteResult` 상태 복원 로직 추가 및 비-VOTE_RESULT 상태 진입 시 `null` 초기화 처리.
  - `handleConfirmVoteResult()` 기존 로컬 상태 전이 코드를 STOMP `publishJson` (`/pub/rooms/{roomCode}/next-round`) 호출로 전면 개편.
- **`GameScreen.jsx`**:
  - `phase === "VOTE_RESULT" && voteResult` 조건을 활용한 라운드 결과 화면 렌더링 유지.

### 4. 다음 작업 (Next Action Items)
- **사망자 투표 상태 버그 디버깅**: 사망한 플레이어에게 투표 버튼이 부적절하게 활성화되는 현상 수정.
- **플레이어 리스트 누락 디버깅**: 게임 시작 시 또는 재시작 시 플레이어 정보 일부가 누락되거나 동기화되지 않는 현상 및 새로고침 후 탈락자가 `players` 리스트에서 아예 사라지는 문제 해결 필요.
- **GAME_OVER 복원 검증**: 게임 종료 화면에서의 새로고침 시 화면 복원 및 대기실 상태 회귀 흐름 추가 분석 및 검증.
