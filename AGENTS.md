# 저장소 작업 지침

## 적용 범위

이 파일은 저장소 전체에 적용된다. 하위 디렉터리에 별도 `AGENTS.md`가 있으면 더 구체적인 하위 지침을 함께 따른다.

## Git 작업

- 작업 브랜치 이름은 `codex`로 시작하지 않는다.
- 새 작업 브랜치는 목적 접두사와 `/`를 사용한다. 예: `feat/기능명`, `fix/버그명`, `style/표현명`, `chore/정리명`, `docs/문서명`, `test/검증명`.
- 새 커밋 메시지는 한글로 작성한다.
- 개인 프로젝트의 검증 완료 작업은 PR 없이 최신 `main`에 직접 병합한다. 병합 전 최신 `main`을 반영하고 전체 테스트와 `git diff --check`를 실행한다.

## 문서 작업

- 문서 전용 요청에서는 React, Vite, npm, JavaScript, JSX, CSS 설정 및 코드 파일을 수정하지 않는다.
- `docs/be_docs/` 폴더는 백엔드 문서 동기화용 원본으로 100% 읽기 전용이다. 절대 수정, 생성, 삭제하지 않고 참조만 수행한다.
- UI 디자인 작업 시 카드 형태 스타일은 최외각 영역(`.panel`, `.modal`)에서만 사용하며, 내부 요소에는 중첩 카드(nested card) 디자인을 사용하지 않는다.
- 경계선 및 구분선은 전달하는 텍스트보다 튀지 않도록 저대비(low contrast), 미세 투명도의 은은한 스타일만 사용한다.
- 문서를 쓰기 전에 관련 소스, 테스트, 빌드 설정과 기존 문서를 확인한다.
- 현재 구현을 설명할 때는 코드와 테스트에서 직접 확인한 사실만 기록한다.
- 계획이나 제안은 현재 동작과 구분하고 `docs/technical-debt.md`에만 기록한다.
- 문서마다 한 가지 책임을 부여하고 같은 설명을 여러 파일에 반복하지 않는다.
- 프로젝트 고유 정보가 없는 일반론, 검색 가능한 공식 링크 모음, 일회성 세션 기록은 만들지 않는다.
- 문서 제목과 파일명은 내용을 구체적으로 드러내게 정한다.
- `docs` 아래 문서 파일명에 `superpowers`를 사용하지 않는다.
- 문서 작업 브랜치 이름에 `codex` 접두사를 사용하지 않는다.
- 문서는 원칙적으로 300줄 이하로 작성한다. 내용을 정확하고 완전하게 설명하는 데 필요하면 300줄을 넘길 수 있다.
- 임의로 300줄을 채우지 않으며 중복, 예시 남발, 오래된 결과, 불필요한 배경 설명을 제거한다.
- 루트에는 저장소 진입점인 `README.md`와 작업 지침인 `AGENTS.md`만 두고, 나머지 프로젝트 문서는 `docs/`에 둔다.
- 완료된 주요 변경은 `docs/change-history.md`에 간결하게 기록한다. 커밋 목록이나 파일별 변경 내역은 Git 이력에 맡긴다.

## 문서 책임

- `README.md`: 프로젝트 개요, 실행 방법, 문서 진입점
- `docs/architecture.md`: 시스템 구조, 상태 소유권, 런타임 경계
- `docs/game-flow.md`: 구현된 게임 규칙과 상태 전이
- `docs/rest-api.md`: HTTP 요청·응답 계약
- `docs/websocket-events.md`: STOMP 명령과 이벤트 계약
- `docs/testing.md`: 자동·수동 검증 방법과 검증 공백
- `docs/technical-debt.md`: 확인된 문제, 영향, 후속 결정과 우선순위
- `docs/change-history.md`: 사용자 관점에서 의미 있는 완료 변경

새 문서는 위 문서에 자연스럽게 포함할 수 없고 독립적으로 유지할 가치가 있을 때만 만든다.

## 문서 변경 절차

1. `README.md`와 관련 문서에서 현재 책임과 링크를 확인한다.
2. 관련 React 컴포넌트, 훅, API 클라이언트/유틸, 상태/이벤트 핸들러, 설정과 테스트를 읽는다.
3. 기존 문서를 수정하거나 통합하는 방법을 새 파일 생성보다 우선 검토한다.
4. 외부 계약이 바뀐 경우 REST와 WebSocket 문서를 함께 확인한다.
5. 현재 제약이나 후속 작업이 달라진 경우 `docs/technical-debt.md`를 갱신한다.
6. 주요 완료 결과라면 `docs/change-history.md`에 한 항목만 추가한다.

## 완료 전 검증

- 변경 목록에 요청 범위를 벗어난 파일이 없는지 확인한다.
- 각 문서의 첫 번째 제목이 파일 내용과 일치하는지 확인한다.
- Markdown 상대 링크가 실제 파일과 제목을 가리키는지 확인한다.
- endpoint, header, STOMP destination, 이벤트명, DTO 필드, enum 값이 현재 코드와 일치하는지 대조한다.
- 문서별 줄 수를 확인하고 300줄 초과 시 그 이유가 내용상 필요한지 검토한다.
- 미완성 표식, 임시 메모, 날짜가 지난 “최근 결과”가 남지 않았는지 확인한다.
- `git diff --check`를 실행한다.
- 문서에 테스트 결과를 기록할 때는 같은 작업에서 새로 실행한 결과만 사용한다.
